packages:
  - 'core/*'
  - 'serialization/*'
  - 'compression/*'
  - 'encryption/*'
  - 'storage/*'
  - 'website'
allowBuilds:
  better-sqlite3: true
  esbuild: true
  msgpackr-extract: true
  sharp: false
  sqlite3: true
  workerd: true
# 7 days minimum release age (minutes). Fail closed if publish time is missing
# or the only matching version is too new.
minimumReleaseAge: 10080
minimumReleaseAgeStrict: true
minimumReleaseAgeIgnoreMissingTime: false
trustPolicy: no-downgrade
strictDepBuilds: true
dangerouslyAllowAllBuilds: false
blockExoticSubdeps: true
# writr@6.1.2 and semver@5.7.2 fail trustPolicy: no-downgrade (no provenance;
# later versions of the same packages have attestations). Pin to versions that
# already meet the gate — do not exclude first-party packages.
overrides:
  writr: 6.1.5
  semver@5.7.2: 7.7.3
