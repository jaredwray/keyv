name: tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [ '12', '14', '16' ]
    name: Node ${{ matrix.node }}
    steps:
      - uses: actions/checkout@v2
      - name: Setup node
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node }}
      
      - name: Start Services
        run: yarn test:services:start

      - name: Install Dependencies  
        run: yarn
        
      - name: Test [keyv]
        run: yarn workspace keyv test

      - name: Test [etcd]
        run: yarn workspace etcd test

      - name: Test [memcache]
        run: yarn workspace memcache test

      - name: Test [mongo]
        run: yarn workspace mongo test

      - name: Test [mysql]
        run: yarn workspace mysql test

      - name: Test [offline]
        run: yarn workspace offline test

      - name: Test [postgres]
        run: yarn workspace postgres test

      - name: Test [redis]
        run: yarn workspace redis test

      - name: Test [sqlite]
        run: yarn workspace sqlite test

      - name: Test [test-suite]
        run: yarn workspace test-suite test

      - name: Test [tiered]
        run: yarn workspace tiered test

      - name: Generate Code Coverage Report
        run: yarn coverage

      - name: Code Coverage
        uses: codecov/codecov-action@v2
        with:
          token: ${{ secrets.CODECOV_KEY }}
          files: ./packages/keyv/coverage.lcov,./packages/mysql/coverage.lcov,./packages/test-suite/coverage.lcov,./packages/sqlite/coverage.lcov,./packages/postgres/coverage.lcov,./packages/mongo/coverage.lcov,./packages/redis/coverage.lcov,./packages/memcache/coverage.lcov,./packages/etcd/coverage.lcov,./packages/offline/coverage.lcov,./packages/tiered/coverage.lcov
